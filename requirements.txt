anthropic>=0.40.0
google-genai>=1.0.0
openai>=1.40.0          # also used for z.ai (OpenAI-compatible endpoint)
truststore>=0.9.0       # use OS trust store (handles corporate TLS-inspecting proxies)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
python-dotenv>=1.0.0
